# OME backend scaffold

This directory exists only to establish architecture boundaries for the harness.

No telemetry feature behavior is implemented during Plan 002.

The planned package boundaries are represented under `src/ome/` so architecture checks can become executable before product features are added.
